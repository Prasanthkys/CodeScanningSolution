#!/bin/bash

# Ensure you are on the Dev3Mnr branch
current_branch=$(git rev-parse --abbrev-ref HEAD)
if [ "$current_branch" = "Dev3Mnr" ]; then
  echo "Pushing to Dev3Mnr branch..."
  git push origin Dev3Mnr --verbose
else
  echo "Not on Dev3Mnr branch. No push performed."
fi
